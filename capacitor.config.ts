import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aniyomi.localsource',
  appName: 'Aniyomi LocalSource',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
